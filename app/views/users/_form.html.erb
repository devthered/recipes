<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation", class="card">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card">
    <div class="card-header">
      <%= form.text_field :name, placeholder: 'your name', class: 'card-title' %>
    </div>
    <div class="card-body">
      <%= form.text_field :email, placeholder: 'email address' %>
      <div class="spacer"></div>
      <%= form.password_field :password, placeholder: 'secure password' %>
      <div class="spacer"></div>

      <div class="actions">
        <%= form.submit %>
      </div>
    </div>
  </div>
<% end %>
